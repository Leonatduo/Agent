以下12道是你必须倒背如流的，建议优先投入80%的精力：

序号	问题	考察原因
1	Agent 和普通 LLM 的本质区别是什么？	Agent面试第一问，必考
LLM是大语言模型， 基于海量数据训练， 可以根据用户提问去思考然后生成用户想要的回答， 但是不具备自主决策，记忆管理相关功能， 相当于大脑的角色， Agent则是以LLM为核心的框架， 在LLM基础上增加了记忆管理， 自主规划， 工具调用等模块，可以根据用户问题自主分析决策是否调用工具以及调用什么工具回答用户问题， 并根据生成的回复进一步引导进一步提问和回答， 同时根据用户提问生成对用户的画像方便后续定制化回复。 

2	ReAct 是什么？和 CoT 有什么区别？	Agent最核心的框架概念
CoT（思维链）是一种提示词工程技巧。它的核心是让模型面对复杂推理题（比如数学逻辑）时，把隐式的推理步骤显式地写出来，即‘一步一步想’，从而减少直接跳步导致的幻觉。但它只依赖模型内部的静态知识。

而ReAct是一种Agent的决策范式，全称是 Reasoning + Acting。它的运行机制是一个紧耦合的循环：模型先思考（Thought）该怎么做，然后执行一个行动（Act）（比如调API），紧接着环境会返回一个观察结果（Observation），模型再根据这个Observation去修正下一步的Thought，直到任务完成。

两者最本质的区别在于： CoT是‘纸上谈兵’，只推理不行动，错了也无法修正；而ReAct是‘实践出真知’，通过与环境交互获取实时反馈，实现了动态纠错。因此在需要调用工具或获取实时信息的Agent场景中，ReAct是标配。”

6	LangGraph 比 LangChain 的 Chain 强在哪里？	几乎所有项目都用LangGraph
LangChain是链式调用， 适合那种确定流程的工作流， 而LangGraph是LangChain的增强版， 在LangChain的基础上增加了循环， 条件分支， State全局状态， 以及 ， 基于这个特性可以支撑更复杂的业务， 在项目中有更好的表现。

8	如何实现 Human-in-the-loop？	企业落地关键，常考
Human-in-the-loop是在agent回答用户问题过程中中断然后让人工干预的这样一个流程， 它是基于checkpointer机制实现的， 具体来说就是langgraph里每个节点的状态都会被持久化到db里， 然后通过interrupt（）阻断，让人工去处理信息，然后再callBack回LangGraph节点继续执行后续的流程

9	Checkpointer 是什么？解决了什么问题？	面试官爱问"状态管理"
Checkpointer是LangGraph的持久化机制， 解决了用户会话信息随着程序的停止而丢失会话信息无法恢复的问题， 而这个机制就是把每个节点执行完的信息都持久化到db里面， 然后方便恢复用户会话状态。同时实现langGraph全过程可追溯


12	短期记忆和长期记忆的区别是什么？	记忆管理核心考点
他们的区别在于存储介质， 生命周期以及用途的不同。
短期记忆是基于内存存储的， 目的是为了保持会话级别上下文的连贯性，  随着会话的关闭也会消失。
长期记忆是持久化到db里存储的， 不会随着会话关闭丢失， 是在在会话中异步对用户习惯沉淀的后台异步工作流， 形成对用户特征的偏好， 

16	HyDE 是什么？有什么优缺点？	RAG最经典的考点之一
HyDE是一种检索方式， 它出现的背景为了解决有时候用户问题太过宽泛， 所以它采用先对用户问题进行一次假设回答， 然后把假设回答作为真正的用户问题输入去回答真正的用户问题。
优点是可以提升文档召回率， 假设回答可以包含更多的专业信息
缺点是不可控， 而且加了一层费token

18	Cross-Encoder 和 Bi-Encoder 的区别？	检索/精排核心概念
Cross-Encoder是跨文档编码， 我理解他主要是为了解决稀疏索引对于专业名词召回效果好，  然后稠密索引对于语义相似度召回效果好， 把两者的优点结合起来按照权重去打分最终召回更全面的信息和文档
Bi-Encoder>

20	什么是检索自省？怎么判断检索是否充分？	解决幻觉的关键手段
检索自省是指对于生成的文档，用别的模型加上设计的系统提示词进行逐句的判断， 判断是否召回文档可以很好的支撑这个问题，  检索主要从问题相关度和事实充分性两个维度来评估， 然后综合给出一个分支。 解决幻觉主要通过强提示词设定来保证。 然后分值低于
某个阈值的时候不会去生成答案

23	用户意图模糊时，缺失信息怎么处理？	面试必问"边界情况"
一级意图不会模糊， 二级意图模糊的时候会引导用户补充， 如果用户问题始终无法识别出对应的意图会自动转人工， 三次引导问答识别不出的话。

31	LLM 生成结果有幻觉怎么办？缓解策略？	企业落地最关注的问题
32	Agent 系统延迟太高，怎么优化？	工程落地必问
二、🟡 次高频题（命中率40-60%，大厂/高阶岗位常问）
这12道属于"加分题"，回答好了能拉开差距：

序号	问题
3	Plan-and-Solve 和 ReAct 的优劣对比
Plan-and-Solve 说白了就是‘先想好再做，一口气干完’。它拿到问题后，先自己规划出完整的解决步骤，然后按步骤一次性把答案算出来，中间不再回头。所以它特别适合那种路径清晰、不太需要外部信息的任务，比如解数学题、逻辑推理。好处是快、省token，坏处是一旦规划错了，那就全错了。

ReAct 刚好相反，它是‘边想边干，干完再看，看完再想’的循环。准确流程是：先思考（Thought）我要干嘛，然后行动（Action）去查工具或算一步，再看观察结果（Observation），根据这个结果再决定下一步怎么想、怎么动，直到最后给出最终答案。它不是‘回答完了再反思’，而是每一步都在循环里动态调整。


4	Tool Calling 和 ReAct 的关系
“Tool Calling 是模型的一种底层能力，就是让模型能输出结构化的工具调用指令，系统去执行函数然后把结果喂回来。

ReAct 是上层的行动策略，它的核心是多轮‘想→做→看→再想’的循环。在这个循环里，当它决定要‘做（Action）’的时候，通常会借助 Tool Calling 来实现工具调用。

5	多 Agent 协作 vs 单 Agent 多工具

7	LangGraph 的 State 和 Reducer 机制
State是LangGraph中在节点间传递的数据共享容器， Reducer是指在State字段上的合并策略， 一般来说多节点在对Graph中的某个字段做修改的时候都是粗暴的新值替换旧值， 但是Reducer机制可以
根据一定策略比如取最大值， 并集的方式替换旧值，其中最经典的是message字段， 通过Reducer采用add_message的方式追加到message中。

10	conditional_edges 怎么用？
conditional_edges是在Graph层面定义一个路由函数， 这个路由函数接收State作为入参， 返回一个字符串或End终止符，这个字符串是下游节点的名称 
使用层面，graph.add_edges("源node", 路由函数)， 定义完跑完之后就知道了该走向哪个下游节点。

13	上下文窗口溢出怎么解决？
17	多路向量检索如何加权合并？
19	并行召回后如何去重筛选？
22	如何评估 RAG 检索质量？有哪些指标？
28	工具调用失败/超时的容错策略？
29	Agent 迷失目标怎么解决？
30	如何评估 Agent 回答质量？
