LangChain 内置多种对话记忆组件，用来简化上下文管理：基础缓存记忆完整存储全部对话上下文；滑动窗口记忆仅保留最近若干轮对话，避免上下文过长；摘要记忆会将早期历史总结压缩，节省 Token 开销；也可自行组合形成混合记忆策略。
这类记忆组件可以绑定到链式任务，自动存取、组装对话历史，替代原生手动拼接字符串的写法。
而向量数据库不属于对话记忆，它是检索增强（RAG）的存储载体，用于存放外部知识库资料，实现跨会话、海量内容的长期知识查询，和聊天上下文记忆是两套不同体系。
